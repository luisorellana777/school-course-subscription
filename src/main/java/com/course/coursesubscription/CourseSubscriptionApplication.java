package com.course.coursesubscription;

import org.springframework.boot.CommandLineRunner;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class CourseSubscriptionApplication implements CommandLineRunner {
	
	public static void main(String[] args) {
		SpringApplication.run(CourseSubscriptionApplication.class, args);
	}
	
	@Override
	public void run(String... args) throws Exception {
		String a = "I remember seeing in either IntellsissssszzzzzzzzzzzzzzzzzzzzzzzzJ or Eclipse the sssetting to " +
				"reformsat (cleanup) fildes " + "whenever thñey are saved. How do I find it (didn't find it in the " +
				"settingsss)";
	}
}
